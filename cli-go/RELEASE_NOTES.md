# CLI Go 0.2.1

Refresh network dependencies and use Go 1.26.6 for release builds. The CLI remains compatible with the existing authentication and deployment workflows. Python packages are unchanged.
